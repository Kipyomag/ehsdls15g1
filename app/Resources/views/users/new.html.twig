{% extends 'base.html.twig' %}



{% block body %}
    <h1>Creation d'utilisateur</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Creation" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('users_index') }}">Retour vers la liste</a>
        </li>
    </ul>
{% endblock %}
