{% extends 'base.html.twig' %}

{% block title %}Panneau d'administration{% endblock %}

{% block body %}
      
    
    <!-- MAIN ARTICLE -->
    <div>
        <div>
            <h1 align="center">Administration</h1>
                <ul>
                    <form action="{{ path('admin_users_search') }}" method="post">
                        <table align="center">
                            <tr align="center"><td><label for="search">Rechercher un utilisateur:</label></td></tr>
                            <tr align="center"><td><input type="search" class="form-control" id="search" name="search" /></td></tr>

                            <input type="hidden" name="_target_path" value="ehs_homepage" />

                            <tr align="center"><td><button type="submit" class="btn btn-primary">Rechercher</button></tr></td>
                        </table>
                    </form>

                    <li>
                        <a href="{{ path('admin_users_new') }}">ajouter un utilisateur</a>
                    </li>

                    <li>
                        <a href="{{ path('admin_listeMembres') }}">Liste des utilisateurs</a>
                    </li>

                    <li>
                        <a href="{{ path('admin_listeArticles') }}">Liste des articles</a>
                    </li>
                    <li>
                        <a href="#">Consulter les archives</a>
                    </li>
                </ul>
        </div>
    </div>
    
    
        
    

{% endblock %}
