{% extends 'base.html.twig' %}
{% block header %}{% endblock %}
{% block body %}
<div class="col-xs-12">
    <h1 class='pageTitle'>Comment edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('comment_index') }}">Back to the list</a>
        </li>
    </ul>
</div>
{% endblock %}
