{% extends 'base.html.twig' %}

{% block title %}Edit archive{% endblock %}

{% block body %}
<div class="col-xs-12">
    <a href="{{ path('archive_index') }}"><span class="glyphicon glyphicon-chevron-left"></span> Retour à la liste</a>
    
    <div class='archiveForm'>
        <h1 class='pageTitle'>Modifier une archive</h1>

        {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input type="submit" value="Edit" />
        {{ form_end(edit_form) }}
    </div>


    {{ form_start(delete_form) }}
        <input type="submit" value="Delete">
    {{ form_end(delete_form) }}
</div>
{% endblock %}
